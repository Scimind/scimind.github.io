(() => {
  const mount = document.querySelector('#home-content');
  if (!mount) return;
  const read = async (name, fallback) => {
    try {
      const response = await fetch(`/assets/data/${name}.json`);
      if (!response.ok) throw new Error(`HTTP ${response.status}`);
      return await response.json();
    } catch (error) {
      console.warn(`SCIMind: ${name}.json could not be loaded.`, error);
      return fallback;
    }
  };
  const safe = (value = '') => String(value).replace(/[&<>"]/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[char]));
  const topicList = topics => Array.isArray(topics) ? `<ul class="topic-list">${topics.map(topic => `<li>${safe(topic)}</li>`).join('')}</ul>` : '';
  const links = item => ['paper','code','project','arxiv'].filter(key => item[key]).map(key => `<a class="text-link" href="${safe(item[key])}" rel="noopener">${key}</a>`).join('');
  Promise.all([
    read('site', { stats: [] }), read('research', []), read('projects', []),
    read('publications', []), read('people', []), read('news', [])
  ]).then(([site, research, projects, publications, people, news]) => {
    mount.innerHTML = `
      <section class="stats" aria-label="Team at a glance"><div class="shell stats__grid">${(site.stats || []).map(item => `<div class="stat reveal"><strong>${safe(item.value)}</strong><span>${safe(item.label)}</span></div>`).join('')}</div><p class="shell stats__note">Together with graduate students and research interns</p></section>
      <section class="section research" id="research" aria-labelledby="research-title"><div class="shell"><header class="section-head reveal"><p class="section-index">02 / Research Directions</p><h2 id="research-title">We study science—and<br><em>how science is discovered.</em></h2><p>From scientific foundation models to real laboratory settings, we build experimental intelligence across computation, simulation, and operation.</p></header><div class="research-grid">${research.map((item,i) => `<article class="research-card research-card--${i+1} reveal"><div class="research-card__signal" aria-hidden="true"><span></span><i></i><b>${safe(item.number)}</b></div><div><p class="card-index">${safe(item.number)}</p><h3>${safe(item.title)}</h3><h4>${safe(item.titleZh)}</h4><p>${safe(item.description)}</p>${topicList(item.topics)}</div></article>`).join('')}</div></div></section>
      <section class="section frontiers" id="projects" aria-labelledby="projects-title"><div class="shell"><header class="section-head section-head--row reveal"><div><p class="section-index">03 / Scientific Frontiers</p><h2 id="projects-title">Science at Scale</h2></div><p>We validate intelligent methods against real scientific problems, aligning model capability with scientific value.</p></header><div class="frontier-list">${projects.map(item => `<article class="frontier reveal"><div class="frontier__visual frontier__visual--${safe(item.id)}" aria-hidden="true"><span>${safe(item.signal)}</span><i></i></div><p class="frontier__index">${safe(item.index)}</p><div><h3>${safe(item.title)}</h3><h4>${safe(item.titleZh)}</h4></div><p>${safe(item.description)}</p></article>`).join('')}</div></div></section>
      <section class="section loop" aria-labelledby="loop-title"><div class="shell"><header class="section-head reveal"><p class="section-index">04 / Experimental Intelligence Loop</p><h2 id="loop-title">Every experiment becomes<br><em>the next step in intelligence.</em></h2></header><div class="loop__stage reveal"><div class="loop__rail" aria-hidden="true"></div>${[['01','Scientific Problem','Define the question'],['02','AI Hypothesis','Generate and model'],['03','World Simulation','Validate virtually'],['04','AI + XR Experiment','Guide real experiments'],['05','Scientific Data','Accumulate evidence'],['06','Model Feedback','Improve the model']].map(([n,en,note]) => `<article class="loop-node"><span>${n}</span><i></i><h3>${en}</h3><p>${note}</p></article>`).join('')}<div class="loop__return"><span>DATA FEEDBACK</span></div></div><p class="loop__formula reveal">Algorithm <i>→</i> Simulation <i>→</i> Experiment <i>→</i> Data <i>→</i> Intelligence</p></div></section>
      <section class="section publications" id="publications" aria-labelledby="publications-title"><div class="shell"><header class="section-head section-head--row reveal"><div><p class="section-index">05 / Selected Research</p><h2 id="publications-title">Representative Work</h2></div><p>Confirmed representative work in world models. Full paper details will be added as source information becomes available.</p></header><div class="publication-list">${publications.length ? publications.map((item,i) => `<article class="publication reveal"><span>0${i+1}</span><div><p>${safe(item.highlight)}</p><h3>${safe(item.title)}</h3></div><strong>${safe(item.venue)} ${safe(item.year)}${item.highlight === 'Oral' ? ' · Oral' : ''}</strong><div>${links(item)}</div></article>`).join('') : '<p class="empty-state">Research updates will appear here.</p>'}</div></div></section>
      <section class="section pi" aria-labelledby="pi-title"><div class="shell pi__grid"><div class="pi__portrait reveal" role="img" aria-label="Principal Investigator portrait placeholder"><span>PI</span><div></div><small>PORTRAIT TO BE UPDATED</small></div><div class="pi__content reveal"><p class="section-index">06 / Principal Investigator</p><h2 id="pi-title">Dajun Zeng</h2><p class="pi__role">Principal Investigator</p><ul class="pi__titles"><li>Deputy Director, Institute of Automation, Chinese Academy of Sciences</li><li>Deputy Director, State Key Laboratory of Multimodal Artificial Intelligence Systems</li><li>IEEE Fellow · AAAS Fellow</li><li>Recipient, National Science Fund for Distinguished Young Scholars</li><li>Chief Scientist, Wenge Group</li></ul><p class="pi__bio">He received his Ph.D. from Carnegie Mellon University in 1998. His research spans artificial intelligence, AI for Science, decision intelligence, multi-agent systems, and social computing. He has played a central role in advancing the Science One scientific foundation model and the “Science One 100” model initiative.</p></div></div></section>
      <section class="section people" id="people" aria-labelledby="people-title"><div class="shell people__grid"><div class="reveal"><p class="section-index">07 / People</p><h2 id="people-title">Defining<br><em>experimental intelligence.</em></h2></div><div class="team-composition reveal"><p>The core team brings together professors, associate and assistant professors, postdoctoral researchers, engineers, and a research assistant, alongside graduate students and interns.</p><div class="team-bars">${[['Professors','2'],['Associate Professor','1'],['Assistant Professor','1'],['Postdoctoral Researchers','3'],['Engineers','2'],['Research Assistant','1']].map(([role,count]) => `<div><span>${role}</span><i style="--count:${count}"></i><b>${count}</b></div>`).join('')}</div>${people.length ? `<div class="member-grid">${people.filter(person => person.name).map(person => `<article><h3>${safe(person.name)}</h3><p>${safe(person.role)}</p></article>`).join('')}</div>` : '<p class="empty-state">Individual team profiles will be added as verified information becomes available.</p>'}</div></div></section>
      <section class="section collaboration" aria-labelledby="collaboration-title"><div class="shell collaboration__inner reveal"><div><p class="section-index">08 / Collaborative Science</p><h2 id="collaboration-title"><strong>40+</strong><br>Collaborating Institutions</h2></div><p>We work with more than 40 institutions to build an interdisciplinary AI for Science innovation network.<span>Institutional profiles will be published when the complete verified list is available.</span></p><div class="collaboration__network" aria-hidden="true">${Array.from({length:12},(_,i)=>`<i style="--i:${i}"></i>`).join('')}</div></div></section>
      <section class="section news" id="news" aria-labelledby="news-title"><div class="shell news__grid"><div class="reveal"><p class="section-index">09 / News</p><h2 id="news-title">Laboratory Updates</h2></div><div class="reveal">${news.length ? news.map(item => `<article class="news-item"><time>${safe(item.date)}</time><h3>${safe(item.title)}</h3><p>${safe(item.summary)}</p></article>`).join('') : '<div class="empty-state empty-state--news"><span>SCIMIND / UPDATES</span><p>Laboratory news and updates will appear here.</p></div>'}</div></div></section>`;
    window.dispatchEvent(new CustomEvent('scimind:content-rendered'));
  });
})();
